CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(MYSHAREDLIB)

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

ADD_LIBRARY(mylib SHARED mylib.cpp)
SET_PROPERTY(TARGET mylib PROPERTY SOVERSION 1.0)