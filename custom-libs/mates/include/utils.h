#ifndef MATES_UTILS_H
#define MATES_UTILS_H

int count_divs(int a, int b, int k);
int rmcd(int a, int b);
int rfact(int N);
bool is_prime(int n);
bool same_prime_divs(int N, int M);


#endif